# Active Context

## Текущий фокус

- Memory Bank полностью заполнен и актуален.
- Ключевые архитектурные решения приняты (см. ниже).
- Проект в стадии **готовности к старту разработки**.
- Следующий шаг — инициализация монорепо.

## Недавние изменения

- Детально описан `projectbrief.md` (686 строк): продукт, 16 разделов функционала, воронка, критерии успеха, ограничения.
- Обновлён `productContext.md`: проблемы ЦА, сценарий happy path, ключевые разделы.
- Обновлён `systemPatterns.md`: архитектура, доменные сущности, воронка статусов, паттерны AI on/off, создание карточек, N8N.
- Обновлён `techContext.md`: полный стек, интеграции, переменные окружения, технические ограничения.
- Обновлён `progress.md`: чеклист разработки.

## Следующие шаги

1. Инициализировать монорепо (Next.js + Node.js API).
2. Настроить Docker Compose (PostgreSQL + N8N + сервисы).
3. Спроектировать схему базы данных (Cards, Contacts, Listings, Events, Orders).
4. Начать с аутентификации / регистрации с одобрением.

## Принятые решения

| Вопрос | Решение |
|---|---|
| Структура репозитория | **Монорепо** (фронтенд + бэкенд в одном репо) |
| AI-модель агента | Не фиксируется в коде — **выбирается и конфигурируется в N8N** |
| Получение сообщений из Avito | Через **N8N** (N8N управляет polling/webhooks, приложение получает события от N8N) |
| N8N | Self-hosted в Docker (в составе проектного Docker Compose) |

## Открытые вопросы

- Нет.
